# Terraform Plan Validator Fixtures

The focused validator fixtures are generated by
`tests/test_terraform_plan_oracle_validator.py`.

They are validator-only fixtures. They are not Domain 3 oracle expected answers,
do not populate `oracle_v1.json`, and do not authorize producer work.

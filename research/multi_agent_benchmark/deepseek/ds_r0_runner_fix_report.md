# DeepSeek DS-R0 Runner Fix Report

## Status

```text
DS_R0_RUNNER_FIX_COMPLETE
STRUCTURED_OUTPUT_INVOCATION_FIX_ONLY
DS_R0_RERUN_NOT_YET_AUTHORIZED
READINESS_NOT_AUTHORIZED
EFFICIENCY_CLAIM_NOT_AUTHORIZED
RELEASE_NOT_AUTHORIZED
```

## Authorization

```text
authorization:
research/multi_agent_benchmark/deepseek/deepseek_ds_r0_runner_fix_authorization.md

authorization commit:
f4577060d22f6313cdead54274e9a131c74676e5
```

## Change

DS-R0 P3 no longer passes a temporary schema file path to Claude Code.

Instead, the runner:

```text
constructs the same probe schema object
serializes it as deterministic compact JSON
passes that JSON string directly to --json-schema
```

## Schema Transport

```text
schema_transport:
INLINE_CANONICAL_JSON

schema_semantics_changed:
false

inline_schema_sha256:
c5bf11b778e9c1167db77772d0fba55dfaad61bd31f829d947ce2f1ab7e2d553

parsed_schema_object_matches_source:
true
```

The inline schema is generated by JSON serialization:

```text
sort_keys: true
separators: "," and ":"
ensure_ascii: true
```

It is not produced by string replacement.

## Schema Invariants

```text
additionalProperties:
unchanged

required fields:
unchanged

enum constraints:
unchanged / not present in probe schema

const constraints:
unchanged
```

## Boundary

```text
benchmark cases sent:
0

readiness sessions:
0

primary/holdout/carryover sessions:
0

benchmark output schema:
unchanged

prompts/cases/frozen inputs:
unchanged

MVP code:
unchanged
```

## Next Gate

This is not DS-R0 acceptance and does not authorize a rerun.

Next required artifact:

```text
research/multi_agent_benchmark/deepseek/deepseek_ds_r0_runner_fix_review.md
```

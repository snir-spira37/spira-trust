# DeepSeek DS-R0 Runner Fix Review

## Status

```text
DS_R0_RUNNER_FIX_ACCEPTED
STRUCTURED_OUTPUT_SCHEMA_TRANSPORT_FIXED
SCHEMA_TRANSPORT_INLINE_CANONICAL_JSON
SCHEMA_SEMANTICS_UNCHANGED
DS_R0_RERUN_AUTHORIZATION_REQUIRED_NEXT
READINESS_NOT_AUTHORIZED
PRIMARY_BENCHMARK_NOT_AUTHORIZED
EFFICIENCY_CLAIM_NOT_AUTHORIZED
RELEASE_NOT_AUTHORIZED
```

## Reviewed Artifact

```text
runner fix commit:
4c3159a

authorization:
research/multi_agent_benchmark/deepseek/deepseek_ds_r0_runner_fix_authorization.md

results:
research/multi_agent_benchmark/deepseek/ds_r0_runner_fix_results.json

report:
research/multi_agent_benchmark/deepseek/ds_r0_runner_fix_report.md
```

## Review Question

```text
Did the runner fix resolve the structured-output invocation defect narrowly,
without changing schema semantics, prompts, cases, frozen inputs, MVP code, or
benchmark thresholds?
```

## Verdict

```text
DS_R0_RUNNER_FIX_ACCEPTED
```

The runner fix is accepted.

DS-R0 P3 now passes JSON Schema as deterministic inline canonical JSON instead
of a schema file path.

## Accepted Fix

```text
schema_transport:
INLINE_CANONICAL_JSON

schema_semantics_changed:
false

inline_schema_sha256:
c5bf11b778e9c1167db77772d0fba55dfaad61bd31f829d947ce2f1ab7e2d553

parsed_schema_object_matches_source:
true
```

The inline schema is generated by JSON parsing/object construction and
deterministic JSON serialization:

```text
sort_keys: true
compact separators
ensure_ascii: true
```

No string replacement is used to fake schema transport.

## Schema Invariants

```text
additionalProperties:
unchanged

required fields:
unchanged

const constraints:
unchanged

enum constraints:
unchanged / not present in probe schema
```

## Validation

```text
focused DS-R0 tests:
9 passed

benchmark asset validator:
PASS

full pytest:
131 passed

secret/private-path scan:
clean

frozen benchmark assets:
unchanged
```

## Still Not Authorized

```text
DS-R0 rerun
9 live readiness sessions
primary benchmark
holdout benchmark
carryover benchmark
MVP changes
producer changes
case or prompt changes
efficiency claim
merge to main
release/version/tag/PyPI
```

## Next Gate

The next artifact must be a fresh DS-R0 rerun authorization:

```text
research/multi_agent_benchmark/deepseek/deepseek_ds_r0_rerun_after_fix_authorization.md
```

The rerun must start from P1:

```text
P1 model resolution
P2 Claude init/tool inventory
P3 structured output
P4 read-only tools
P5 write/web/subagent denial
P6/P7 session isolation
P8 usage accounting
```

Only a future passing DS-R0 result and review may authorize the nine readiness
sessions.
